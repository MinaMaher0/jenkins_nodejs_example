apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: "namespace-value"
spec:
  selector:
    app : "mysql-namespace-value"
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306